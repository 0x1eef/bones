#!/bin/sh
set -e

##
# vars
basename="${1}"
sourcedir="${XDG_DATA_HOME:-${HOME}/.local/share}/bones/${basename}"

##
# main
if [ -z "${basename}" ]; then
    echo "bones: provide the name of a skeleton"
    exit 1
fi
cd "${sourcedir}"
git pull --rebase origin "$(git branch --show-current)"
