
#!/bin/sh
set -e

##
# globals
localbase="${LOCALBASE:-/usr/local}"
user="_bones"
bindir=""

##
# functions
bindir()
{
    if [ "$(uname)" = "Linux" ]; then
        echo "/bin"
    else
        echo "${localbase}"/bin
    fi
}

##
# main
doas -u "${user}" -n -- "$(bindir)"/git "${@}"
exit "${?}"
